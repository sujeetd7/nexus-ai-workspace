/**
 * Root contract test entry — delegates to @nexus/openapi package tests.
 * Run: pnpm --filter @nexus/openapi test
 */
describe("contract suite placeholder", () => {
  it("documents that contract tests live in @nexus/openapi", () => {
    expect(true).toBe(true);
  });
});
